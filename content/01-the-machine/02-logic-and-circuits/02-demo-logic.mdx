---
title: "Try a logic gate"
module: the-machine
concept: logic-and-circuits
order: 2
type: interactive
---

import { LogicGateDemo } from "@/components/interactive/logic-gate-demo";

## Change the inputs. Watch the rule.

The diagram below has two inputs, A and B. Each input can be 0 or 1. The gate applies one rule and gives one answer.

<LogicGateDemo />

When you change the gate type, the wires stay the same. Only the rule changes.
