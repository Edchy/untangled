---
title: "Binary"
module: the-machine
concept: the-foundation
order: 16
cols: 2
type: text
component: switch-illustration
---
Each transistor can hold **a bit**. Short for binary digit. Bi as in two. Digit as in a single number.

Imagine having a computer with only one transistor. It could only hold one bit then, and therefore only be able to represent **two numbers**.

`0` (off)
`1` (on).
